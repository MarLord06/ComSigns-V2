import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Temporarily disable StrictMode to debug multiple registrations
  reactStrictMode: false,
};

export default nextConfig;
